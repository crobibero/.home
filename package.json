{
  "scripts": {
    "build:html": "cross-env TZ=UTC resumed render --output index.html",
    "build:pdf": "cross-env TZ=UTC resumed export resume.json --output resume.pdf --puppeteer-arg=--no-sandbox",
    "validate": "resumed validate"
  },
  "dependencies": {
    "jsonresume-theme-full": "^0.0.4",
    "puppeteer": "^24.36.1",
    "resumed": "^6.1.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
