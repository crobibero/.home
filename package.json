{
  "scripts": {
    "build": "cross-env TZ=UTC resumed render --output index.html",
    "validate": "resumed validate"
  },
  "dependencies": {
    "jsonresume-theme-full": "^0.0.4",
    "resumed": "^3.0.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
